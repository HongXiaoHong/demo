server:
  port: 8001

spring:
  application:
    name: nacos-demo
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: demo
        prefix: test